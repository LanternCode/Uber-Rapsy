ALTER TABLE `log` CHANGE `Description` `Description` TEXT CHARACTER SET utf8mb4 COLLATE utf8mb4_polish_ci NOT NULL;

ALTER TABLE `song` ADD `SongBelTen` BIT(1) NOT NULL DEFAULT b'0' COMMENT '<10 button' AFTER `SongBelow`, ADD `SongBelNine` BIT(1) NOT NULL DEFAULT b'0' COMMENT '<9 button' AFTER `SongBelTen`, ADD `SongBelEight` BIT(1) NOT NULL DEFAULT b'0' COMMENT '<8 button' AFTER `SongBelNine`, ADD `SongBelFour` BIT(1) NOT NULL DEFAULT b'0' COMMENT '<4 button' AFTER `SongBelEight`, ADD `SongDuoTen` BIT(1) NOT NULL DEFAULT b'0' COMMENT '\"10\" button' AFTER `SongBelFour`, ADD `SongVeto` BIT(1) NOT NULL DEFAULT b'0' COMMENT 'Veto button' AFTER `SongDuoTen`;

ALTER TABLE `list` ADD `btnRehearsal` BIT(1) NOT NULL DEFAULT b'1' AFTER `ListActive`, ADD `btnDistinction` BIT(1) NOT NULL DEFAULT b'1' AFTER `btnRehearsal`, ADD `btnMemorial` BIT(1) NOT NULL DEFAULT b'1' AFTER `btnDistinction`, ADD `btnXD` BIT(1) NOT NULL DEFAULT b'1' AFTER `btnMemorial`, ADD `btnNotRap` BIT(1) NOT NULL DEFAULT b'1' AFTER `btnXD`, ADD `btnDiscomfort` BIT(1) NOT NULL DEFAULT b'1' AFTER `btnNotRap`, ADD `btnTop` BIT(1) NOT NULL DEFAULT b'1' AFTER `btnDiscomfort`, ADD `btnNoGrade` BIT(1) NOT NULL DEFAULT b'1' AFTER `btnTop`, ADD `btnUber` BIT(1) NOT NULL DEFAULT b'1' AFTER `btnNoGrade`, ADD `btnBelowTen` BIT(1) NOT NULL DEFAULT b'1' AFTER `btnUber`, ADD `btnBelowNine` BIT(1) NOT NULL DEFAULT b'1' AFTER `btnBelowTen`, ADD `btnBelowEight` BIT(1) NOT NULL DEFAULT b'1' AFTER `btnBelowNine`, ADD `btnBelowSeven` BIT(1) NOT NULL DEFAULT b'1' AFTER `btnBelowEight`, ADD `btnBelowFour` BIT(1) NOT NULL DEFAULT b'1' AFTER `btnBelowSeven`, ADD `btnDuoTen` BIT(1) NOT NULL DEFAULT b'1' AFTER `btnBelowFour`, ADD `btnVeto` BIT(1) NOT NULL DEFAULT b'1' AFTER `btnDuoTen`;

CREATE TABLE `lanternc_uberrapsy`.`report` (`reportId` INT(10) NOT NULL AUTO_INCREMENT , `reportText` TEXT CHARACTER SET utf8 COLLATE utf8_polish_ci NOT NULL , PRIMARY KEY (`reportId`)) ENGINE = MyISAM;
